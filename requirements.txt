# RAG API and pipeline dependencies
# Use: pip install -r requirements.txt

# RAG REST API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# RAG pipeline (load/split/vector store)
pypdf>=4.0.0
python-dotenv>=1.0.0
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain-chroma>=0.1.0
langchain-ollama>=0.1.0

# Gradio chat UI (Phase 4)
gradio>=4.0.0

# Phase 3B - Zero-shot intent classifier (optional, adds ~1s per gray-zone query)
transformers>=4.30.0
torch>=2.0.0

# Remote LLM provider GLM (optional, for RAG_LLM_PROVIDER=glm)
zhipuai>=2.0.0

# Evaluation visualization (Phase 2.1)
# umap-learn>=0.5.0  # Optional: better 2D projection; falls back to PCA if not installed
plotly>=5.0.0

# SP-API Agent (Task 000007)
langgraph>=0.1.0
redis>=5.0.0
sse-starlette>=1.8.0
httpx>=0.25.0

# Test dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
hypothesis>=6.0.0
requests>=2.28.0
