List each distinct sub-question from the user query.

Output JSON only:
- {"intents": ["...", "..."]}

Rules:
- Do NOT merge multiple questions into one.
- Each sub-question must be a separate item.
- Preserve ASINs, order IDs, dates exactly.
- Do NOT split dates that contain commas (e.g. keep "September 1st, 2026" or "January 1, 2025" as one intent, not two).

Examples:
- "what is FBA get order 123 which table"
  -> {"intents": ["what is FBA", "get order 123", "which table stores referral fee data"]}
- "what is FBA vs FBM, get order 112-123, which table stores fee, show storage trend"
  -> {"intents": ["what is the difference between FBA and FBM", "get order status for 112-123", "which ClickHouse table stores referral fee breakdown", "show me month over month trend of storage fees"]}