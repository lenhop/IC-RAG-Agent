Split the user query into distinct, independent sub-questions.
Output ONLY valid JSON. No extra text, no explanation, no markdown.

Output format:
{"intents": ["question 1", "question 2"]}

Rules:
1. Do NOT merge questions. Keep separate.
2. Preserve all entities: ASINs, Order IDs, dates, numbers.
3. DO NOT split date strings with commas (e.g., "January 1, 2025" stays whole).
4. Do not add or remove information.

Examples:
Input: "what is FBA get order 123 which table"
Output: {"intents": ["what is FBA", "get order 123", "which table stores referral fee data"]}

Input: "what is FBA vs FBM get order 112-123 show storage trend"
Output: {"intents": ["what is the difference between FBA and FBM", "get order status for 112-123", "show me month over month trend of storage fees"]}